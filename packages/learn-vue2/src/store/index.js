import Vue from "vue";
import Vuex from "vuex";
import jsonServerService from '../services/jsonServer'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  getters: {},
  modules: {},
});
